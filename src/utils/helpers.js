const hello = (name) => {
  console.log(`Hello there ${name}`);
};

const secret = (name) => {
  console.log(`Hello secret ${name}`);
};

/**
 Todo: 
  Create an export default of `hello`
 */

module.exports = hello;